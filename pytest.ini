[pytest]
# COG test harness (bootstrapped by P1-0 in COG-IMPLEMENTATION-PLAN.md).
# Run: .venv/bin/pytest -q
testpaths = tests
addopts = -q
# Tests must not hit the network or external APIs by default — mock those.
