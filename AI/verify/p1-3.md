# Verify — P1-3 Orchestration doctrine + skill-shape adoption

**Item:** P1-3 · **Branch:** `cog/p1-orchestration-doctrine` · **Date:** 2026-07-29 · **Result:** ✅ PASS (docs-only)

## What was built
- **New** `04-knowledge/patterns/agent-orchestration-patterns.md` — states COG's governing rule (lead orchestrates; workers don't call workers; fan-out ends in a named merge), the five named patterns (Direct / Single-worker skill / Parallel fan-out + merge / Sequential user-driven pipeline / Research isolation), four named anti-patterns (incl. council/debate for routine work), the endorsed verification-subagent upgrade, and a "when to add a new orchestrated workflow" bar. Notes the flat topology is a COG choice, not a platform limit (subagents can nest since v2.1.172).
- `CLAUDE.md` — new `### Orchestration Doctrine` subsection: one paragraph + link to the reference doc.
- `.claude/skills/{compress,ingest,decide}/SKILL.md` — each gained a **Common Rationalizations** table (excuse → why-wrong) and an evidence-based **Verification** checklist. These are the three shortcut-prone skills (a lazy pass can look done but isn't).

## Verification (docs-only — no runtime behavior to exercise)
```
1. CLAUDE.md link → 04-knowledge/patterns/agent-orchestration-patterns.md → target EXISTS ✓
2. CLAUDE.md "### Orchestration Doctrine" present (line 101) ✓
3. compress: Rationalizations=1 Verification=1 ✓
   ingest:   Rationalizations=1 Verification=1 ✓
   decide:   Rationalizations=1 Verification=1 ✓
4. reference doc contains all 5 pattern names ✓
```

## Notes / risk
- **Zero runtime behavior change** — pure documentation/instruction additions. No script, config, or graph touched.
- No committed test (correct per plan: docs/config items are verification-only).
- CLAUDE.md edit here is against the `main-local` version; the P1-0 branch's separate `## Testing` addition touches a different section, so both merge cleanly.
