# Verify — P1-0 Bootstrap pytest + tests/ scaffolding

**Item:** P1-0 · **Branch:** `cog/p1-test-bootstrap` · **Date:** 2026-07-29 · **Result:** ✅ PASS

## What was built
- `requirements-dev.txt` — pins `pytest==9.1.1` (dev-only dep).
- `pytest.ini` — `testpaths = tests`, `-q`, no-network policy note.
- `tests/conftest.py` — puts `scripts/` on `sys.path`; fixtures `repo_root`, `sample_graph`, `sample_graph_file`, `sample_config`.
- `tests/test_smoke.py` — 3 smoke tests (janitor imports + `load_config` runs; `sample_graph` schema-accurate; on-disk fixture round-trips).
- `tests/README.md` — run instructions + conventions.
- `CLAUDE.md` — added a `## Testing` section (runner + policy pointer).

## Environment
```
$ .venv/bin/python -c "import pytest; print(pytest.__version__)"
9.1.1
```

## Test run (the deliverable's self-check)
```
$ .venv/bin/pytest -q
...
3 passed in 0.02s
```

## Verification criteria (from plan)
- [x] `.venv/bin/pytest -q` → passing, 0 errors (3 passed).
- [x] Smoke test imports a core module (`janitor.load_config`) and asserts it runs.
- [x] Fixtures load (`sample_graph`, `sample_graph_file` exercised by the smoke tests).

## Notes / risk
- Zero production-code behavior change — additive scaffolding only. `CLAUDE.md` gained a docs section.
- `sample_graph` fixture is deliberately seeded (owns_by/owned_by pair, singleton rel type, near-dup Person pair, hub node) so P1-2 / P1-6 / P2-1 / P3-1 reuse it without rebuilding.
